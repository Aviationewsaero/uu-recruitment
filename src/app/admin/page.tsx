export default function AdminPage() {
  return (
    <main className="flex-1">
      <div className="mx-auto max-w-2xl px-6 py-16">
        <h1 className="text-3xl font-bold text-brand-text">Admin Console</h1>
        <p className="mt-3 text-brand-muted">
          Staff login + admin dashboard — Day 3 (auth + roles), Day 6 (student
          management, bulk email, analytics tiles).
        </p>
      </div>
    </main>
  );
}
